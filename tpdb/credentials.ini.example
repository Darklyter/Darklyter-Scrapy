# Copy to credentials.ini and fill in.  credentials.ini is gitignored.
# Any value here can be overridden per-run with:
#   scrapy crawl <spider> -a username=... -a password=...

[adulttime]
username =
password =
